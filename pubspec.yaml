name: borneogis_navigator
description: BorneoGIS Navigator - Field mapping app by Lamri.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  location: ^6.0.2
  flutter_compass: ^0.8.0
  file_picker: ^8.1.2
  path_provider: ^2.1.4
  path: ^1.9.0
  provider: ^6.1.2
  shared_preferences: ^2.3.3
  permission_handler: ^11.3.1
  cupertino_icons: ^1.0.8
  xml: ^6.5.0
  uuid: ^4.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
